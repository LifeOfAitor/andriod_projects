<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.RegistrationForm" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your default colors here -->

        <!-- Primary branding color -->
        <item name="colorPrimary">#2196F3</item> <!-- blue -->
        <item name="colorOnPrimary">#FFFFFF</item>

        <!-- Secondary (used for accents) -->
        <item name="colorSecondary">#03DAC5</item>
        <item name="colorOnSecondary">#000000</item>

        <!-- Background and surface -->
        <item name="colorSurface">#121212</item>
        <item name="colorOnBackground">#FFFFFF</item>
        <item name="colorOnSurface">#FFFFFF</item>

        <!-- Error color -->
        <item name="colorError">#CF6679</item>
        <item name="colorOnError">#000000</item>

        <!-- Button styles -->
        <item name="materialButtonStyle">@style/CustomButton</item>
    </style>

    <style name="CustomButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">#2196F3</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="SpinnerTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:popupBackground">@android:color/white</item>
    </style>

</resources>